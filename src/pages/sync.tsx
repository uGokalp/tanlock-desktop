import Sortable from "@/components/Sync/Sortable"
import Sidebar from "@/layouts/Sidebar"

const Syncronize = () => {
  return (
    <Sidebar>
      <Sortable />
    </Sidebar>
  )
}

export default Syncronize
